import { privateMetadata } from '@/lib/seo';
export const metadata = privateMetadata('Loyalty account', '/loyalty');
export default function LoyaltyLayout({ children }: { children: React.ReactNode }) { return children; }
