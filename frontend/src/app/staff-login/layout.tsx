import { privateMetadata } from '@/lib/seo';
export const metadata = privateMetadata('Staff sign in', '/staff-login');
export default function StaffLoginLayout({ children }: { children: React.ReactNode }) { return children; }
